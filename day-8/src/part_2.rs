pub fn process(input: &str) -> usize {
    todo!()
}

#[cfg(test)]
mod tests {
    use rstest::rstest;

    use super::*;

    #[rstest]
    fn test_process() {
        // given
        let input = "";

        // when
        let output = process(input);

        // then
        assert_eq!(output, todo!())
    }
}
